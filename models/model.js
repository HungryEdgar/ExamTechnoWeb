class Room {
    constructor(name, length, width, surface) {
        this.name = name;
        this.length = length;
        this.width = width;
        this.surface = surface;
    }
}

module.exports = Room;